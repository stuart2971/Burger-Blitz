function initKitchen() {
    image(kitchenImage, 0, 0, windowWidth, windowHeight);

    // Plate
    stroke(0);
    fill(color(255, 255, 255));
    // ellipse(windowWidth / 2, (windowHeight / 3) * 2 + 100, 100, 100);
    // ellipse(windowWidth / 2, (windowHeight / 3) * 2 + 100, 40, 40);
    // image(
    //     plateImage,
    //     windowWidth / 2 - 75,
    //     (windowHeight / 3) * 2 + 25,
    //     150,
    //     150
    // );
}
